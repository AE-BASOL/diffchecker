# Phase 2 Summary

- Updated row background and border colors in diff view (`.aligned-cell`, `.diff-cell`) to align with the new dark theme.
- Updated line numbers (`.line-no`, `.aligned-no`) to use muted text and proper borders.
- Re-styled inline `.mark.insert`, `.mark.delete`, `.mark.change` tags with alpha backgrounds and white text for better contrast.
- Ensured empty cells display as `var(--panel-dark)` instead of hardcoded white/grey.

Phase 2 is complete.
